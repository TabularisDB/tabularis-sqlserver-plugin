[package]
name = "sqlserver-plugin"
version = "0.1.0"
edition = "2021"
description = "Tabularis driver plugin for Sqlserver"
license = "Apache-2.0"
publish = false

[dependencies]
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"
